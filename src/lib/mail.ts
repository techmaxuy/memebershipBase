
export const sendVerificationEmail = async (email: string) => {
    console.log(`[Mail Stub] Sending verification email to ${email}`);
    // Future implementation: Send email using a provider (Resend, SendGrid, etc.)
    return true;
}
